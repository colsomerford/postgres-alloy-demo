shared_preload_libraries = 'pg_stat_statements'
compute_query_id = on
pg_stat_statements.track = all
track_activity_query_size = 4096
listen_addresses = '*' # Listen on all network interfaces to allow external access (essential for Docker)
